# a small internal tool

## Installation

1. Install bun from [the official site](https://bun.com/).
2. Open terminal in this folder and run `bun i`.

## Running a tool

### Updating localization

In terminal from this directory run:

```bash
bun run index.ts
```
